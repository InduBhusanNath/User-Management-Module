let pwd=data.password;
          bcrypt.compare(originalPassword,pwd,function(err,result){
                  if(result){
                         
                         {
                                                             
                             
                         });
                  }else{
                         res.send({"flag":"0"});
                  }
          });